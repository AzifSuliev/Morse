@page
@model Index1Model
<form method="post" id="translateForm" novalidate>
    <h2>Morse --> Abc</h2>
    <div class="row">
        <div class="row col-6 px-4">
            <label for="textAreaInput">Type your text:</label>
            <textarea id="textAreaInput" style="font-size:28px; white-space: nowrap;" name="InputText" rows="5" cols="50" placeholder="Type..." onkeydown="handleKeyPress(event);">@Model.InputText</textarea>
            <input id="translate-button" type="submit" value="Translate">
        </div>
        <div class="row col-6 px-4">
            <label for="textAreaOutput">Result:</label>
            <textarea id="textAreaOutput" name="OutputText" readonly style="font-size:28px; rows="5" cols="50">@Model.OutputText</textarea>
        </div>
    </div>
</form>

<script>
    function handleKeyPress(event) {
        if (event.key === 'Enter') {
            event.preventDefault();
            document.getElementById('translateForm').submit();
            return false;
        }
    }
</script>
